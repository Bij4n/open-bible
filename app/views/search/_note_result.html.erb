<li class="rounded-lg border border-surface-200 bg-surface-50 p-3 dark:border-surface-800 dark:bg-surface-900">
  <header class="flex items-start justify-between gap-3">
    <p class="font-ui text-xs uppercase tracking-wider text-surface-500 dark:text-surface-400">
      <%= note.highlights.map(&:osis_ref).join(", ") %>
      <span class="ml-2 italic normal-case tracking-normal">
        <%= t("search.by_author", name: note.user.author_name) %>
      </span>
    </p>
    <% if note.upvote_count.positive? %>
      <span class="font-ui text-xs text-surface-600 dark:text-surface-400">
        <%= t("search.upvotes", count: note.upvote_count) %>
      </span>
    <% end %>
  </header>
  <p class="mt-1 font-reading text-sm text-surface-900 dark:text-surface-100">
    <%= highlight_terms(note.body.to_plain_text, query) %>
  </p>
</li>
