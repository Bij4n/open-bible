<%= turbo_frame_tag dom_id(@note) do %>
  <article class="rounded-lg border border-surface-200 bg-surface-50 p-5 dark:border-surface-800 dark:bg-surface-900">
    <header class="mb-3 flex items-start justify-between gap-2">
      <%# osis_ref eyebrow goes mono — this is the canonical "verse
          ref" use case for the JetBrains Mono token (refs like
          "Bible.KJV.John.3.16" are code-like, no proper-noun
          ambiguity). %>
      <p class="font-mono text-xs uppercase tracking-widest text-surface-500 dark:text-surface-400">
        <%= @note.highlights.map(&:osis_ref).join(", ") %>
      </p>
    </header>

    <div class="trix-content font-reading text-surface-900 dark:text-surface-100">
      <%= @note.body %>
    </div>

    <%= render partial: "comments/thread", locals: { note: @note } %>
  </article>
<% end %>
