<% content_for :title, t("groups.new") %>

<article class="mx-auto max-w-xl space-y-6">
  <h1 class="font-ui text-3xl font-semibold tracking-tight text-surface-900 dark:text-surface-50">
    <%= t("groups.new") %>
  </h1>

  <%= render "form", group: @group %>
</article>
